# SketchyBar Buildkite Plugin - Environment Variables
#
# This file is OPTIONAL. The plugin uses gh CLI for authentication.
# Copy to .env only if you need to override defaults:
#   cp env.example .env

# Enable debug logging (logs to ~/tmp/sketchybar-cache/buildkite.log)
#DEBUG=1
